# ME Agent-Sim Fixtures

These fixtures simulate intended Codex skill behavior without calling an
LLM. The Rust integration tests load them and execute deterministic ME
engine operations where mutation is expected.
